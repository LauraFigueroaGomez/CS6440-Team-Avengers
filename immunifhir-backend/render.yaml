services:
  - type: web
    name: immunifhir-backend
    env: python
    rootDir: immunifhir-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    plan: free
